# Environment Variables Configuration
# Copy this file to .env.local and fill in the values

# ============================================
# ADMIN PANEL (REQUIRED)
# ============================================
# Legacy admin password for super admin access
# Used for backward compatibility and emergency admin access
# Minimum 8 characters recommended
ADMIN_PASSWORD=your_secure_password_here

# ============================================
# SITE CONFIGURATION (REQUIRED)
# ============================================
# Full URL of your site (no trailing slash)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================
# JWT AUTHENTICATION (REQUIRED)
# ============================================
# Secret key for JWT token signing
# MUST be at least 32 characters long
# Generate with: openssl rand -base64 32
# Keep this secret and never commit to version control
JWT_SECRET=generate_with_openssl_rand_base64_32

# ============================================
# SESSION CONFIGURATION (OPTIONAL)
# ============================================
# Session duration in seconds
# Default: 604800 (7 days)
# 1 day = 86400, 7 days = 604800, 30 days = 2592000
SESSION_DURATION=604800

# ============================================
# VERCEL BLOB STORAGE (AUTO-CONFIGURED)
# ============================================
# Token for Vercel Blob Storage access
# Auto-generated by Vercel in production
# Leave empty for local development (uses local JSON file instead)
# DO NOT set manually - Vercel will handle this
BLOB_READ_WRITE_TOKEN=

# ============================================
# VERCEL POSTGRES (AUTO-CONFIGURED)
# ============================================
# PostgreSQL database connection strings
# Auto-generated by Vercel when you enable Postgres
# For local development, use your local Postgres connection string
# Format: postgres://user:password@host:port/database
#
# If not set, system falls back to legacy admin-only mode (no user auth)
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=

# ============================================
# DEPLOYMENT CHECKLIST
# ============================================
# Before deploying to Vercel:
# 1. Set ADMIN_PASSWORD (strong password)
# 2. Set JWT_SECRET (generate with: openssl rand -base64 32)
# 3. Set NEXT_PUBLIC_SITE_URL (your production domain)
# 4. Enable Vercel Postgres in dashboard (auto-sets POSTGRES_URL)
# 5. Enable Vercel Blob in dashboard (auto-sets BLOB_READ_WRITE_TOKEN)
# 6. After first deploy, run: npm run db:init
# 7. After db:init, run: npm run create-admin
# 8. Test login at: yourdomain.com/admin

# ============================================
# REQUIRED vs OPTIONAL
# ============================================
# REQUIRED (must be set):
#   - ADMIN_PASSWORD
#   - JWT_SECRET
#   - NEXT_PUBLIC_SITE_URL
#
# REQUIRED FOR USER AUTH (optional if you want admin-only mode):
#   - POSTGRES_URL
#   - POSTGRES_URL_NON_POOLING
#
# OPTIONAL (have defaults):
#   - SESSION_DURATION (defaults to 7 days)
#
# AUTO-CONFIGURED (don't set manually):
#   - BLOB_READ_WRITE_TOKEN (set by Vercel in production)
